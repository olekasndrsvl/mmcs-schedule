<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MMCSSCHEDULE" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="appBackground">@color/background_dark</item>
        <item name="appForeground">@color/foreground_dark</item>
        <item name="appCardBackground">@color/card_background_dark</item>
        <item name="appTextPrimary">@color/text_primary_dark</item>
        <item name="appTextSecondary">@color/text_secondary_dark</item>
        <item name="appAccent">@color/accent_dark</item>
        <item name="mmcsLogo">@drawable/mmcs_logo_dark</item>
        <item name="blackOnLight">@color/white</item>
        <item name="whiteOnLight">@color/black</item>
        <!-- Material 3 colors for dark theme -->
        <item name="colorPrimary">@color/card_background_dark</item>
        <item name="colorOnPrimary">@color/text_primary_dark</item>
        <item name="colorPrimaryContainer">@color/card_background_dark</item>
        <item name="colorSecondary">@color/accent_dark</item>
        <item name="colorOnSecondary">@color/foreground_dark</item>
        <item name="android:statusBarColor">@color/card_background_dark</item>
    </style>
</resources>